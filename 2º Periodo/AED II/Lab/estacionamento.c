#include <stdio.h>
#include <stdbool.h>
int calculo(){
	int k,n,i;
	scanf("%d %d",&k, &n);
	int *C[n], *S[n], *v1[k], *v2[k];
	while(i<n){
		scanf("%d", & *(C[i]));
		scanf("%d", & *(S[i]));
		i++;
	}
	while(k){
                v[i] = C[i];
       		v2[i] = S[i];
	}
	bool resp = false;
	if(v2[k - 1] < C[n - 1]) resp = true;
}
int main(){
	calculo();
	return 0;
}
